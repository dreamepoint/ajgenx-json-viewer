
class JSONViewer {
    constructor() {
        this.container = document.createElement('pre');
    }
    getContainer() {
        return this.container;
    }
    showJSON(json, collapse, indent) {
        this.container.textContent = JSON.stringify(json, null, indent || 2);
    }
}
